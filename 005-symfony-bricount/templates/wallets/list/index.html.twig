{% extends '_layout/in_app.html.twig' %}

{% block title %}Mes Portefeuilles{% endblock %}

{% block content %}
    <div class="container mt-5">
        {# En-tête de la page #}
        <div class="page-header">
            <div>
                <h1 class="page-title">Mes Portefeuilles</h1>
                <p class="text-muted">Gérez vos dépenses communes simplement.</p>
            </div>
            <a href="{{ path('wallets_create') }}" class="btn btn-create">
                <span class="icon">+</span> Nouveau portefeuille
            </a>
        </div>

        <div class="row g-4"> {# g-4 ajoute de l'espace entre les colonnes #}
            {% if wallets is empty %}
                <div class="col-12">
                    <div class="empty-state">
                        <div class="empty-icon">📂</div>
                        <h3>C'est un peu vide ici</h3>
                        <p>Vous n'avez pas encore de portefeuille. Créez-en un pour commencer !</p>
                    </div>
                </div>
            {% else %}
                {% for wallet in wallets %}
                    <div class="col-md-6 col-lg-4">
                        {# Début de la carte personnalisée #}
                        <div class="wallet-card">
                            <div class="wallet-card-header">
                                <h5 class="wallet-label">{{ wallet.label }}</h5>
                                <span class="wallet-badge member">Membre</span>
                            </div>

                            <div class="wallet-card-body">
                                <div class="wallet-balance-section">
                                    <span class="balance-label">Dépenses totales</span>
                                    <span class="balance-amount">
                                        {{ (wallet.totalAmount / 100)|number_format(2, ',', ' ') }} <small>€</small>
                                    </span>
                                </div>
                            </div>

                            <div class="wallet-card-footer">
                                <a href="{{ path('wallets_show', {uid: wallet.uuid}) }}" class="btn-details">
                                    Voir le détail →
                                </a>
                            </div>
                        </div>
                        {# Fin de la carte #}
                    </div>
                {% endfor %}
            {% endif %}
        </div>
    </div>
{% endblock %}
